# 前日リマインド自動化（第一営業部）

オンライン商談アポの**前日リマインド電話**を自動化する仕組み。

## 解こうとしている問題

| | Before | After |
|---|---|---|
| 対象 | 1日 約15件のアポ | 同じ |
| 手段 | 全件に人が電話 | メール全件自動 ＋ 電話は絞った3〜5件 |
| 工数 | **約2時間/日** | **約30分/日**（メール確認10分＋架電20分） |

電話を全廃するのではなく、**電話が効く先だけ電話に残す**ハイブリッド構成。
朝イチ枠・来社/訪問・重要案件・連絡先がメールしか無い先は、抑止力が落ちるので人が架けます。

## 仕組み

```
毎日 17:00 (JST)
   │
   ├─ 第一営業部6名のGoogleカレンダーから翌営業日の予定を取得
   │     urashima / k_tsuchiya / t_kanno / murata / k_yoshikawa / enomoto
   │
   ├─ 社内MTGを除外し、顧客アポだけを抽出（TimeRexのdescriptionをパース）
   │     会社名・氏名・メール・電話・業種・課題・Meet URL
   │     同席案件は event.id で重複排除し、担当を1人に確定
   │
   ├─ 架電優先度を判定
   │     P1（人が架電）… メール無し / 11時前開始 / 来社・訪問・★・キックオフ・納品・2回目以降
   │     P2（メールのみ）… それ以外
   │
   ├─ メールアドレスが取れた全件について Gmail下書きを作成
   │     To=顧客、Cc=担当営業、文面は商談種別ごとのテンプレート
   │
   └─ 架電リストを出力（担当ごと・電話番号・架電理由・話の材料つき）
```

## 構成

```
config/team.json                              設定。対象メンバー・架電条件・自動送信フラグ
.claude/skills/apo-remind/
    SKILL.md                                  実行手順の本体
    references/extraction.md                  カレンダー → 顧客情報 の抽出仕様
    references/mail-templates.md              メール文面テンプレート（種別A〜E）
    references/call-list.md                   架電リストの出力書式
docs/runbook.md                               日次運用・切替手順・トラブルシュート
```

## 使い方

**手動実行**（任意の日を対象にできる）

```
明日のリマインドやって
8/10のリマインドリスト作って
```

`apo-remind` スキルが起動し、下書き作成と架電リスト出力まで走ります。

**自動実行**

毎日 17:00 (JST) の Routine から起動します。日〜木に発火し、翌営業日を対象にします。
設定・停止方法は `docs/runbook.md` を参照。

## いまの制約（重要）

- **Gmail下書きは `aizawa@writeup.co.jp` にしか作れません。** Gmail APIは認証済みアカウントにしか書き込めないためです。
  現状は「1アカウントから送信・Ccに担当営業・署名は担当営業名」で運用します。
  各営業が自分のアドレスから送りたい場合の対応は `docs/runbook.md` の「送信元アカウントの本番構成」を参照。
- 営業が手動でカレンダーに入れた予定（TimeRex経由でない予定）は顧客情報が無いため自動化に乗りません。
  8/4のデータでは24件中4件が該当。**手動作成時に `ゲスト会社名 : ` `ゲスト氏名 : ` `ゲストメールアドレス : ` の3行を書く運用**にすれば解消します。
- まずは `delivery.autoSend: false`（下書きまで）で運用してください。2週間ほど内容を確認し、問題なければ `true` に変えると送信まで自動になります。

## 電話をAI音声で自動化したい場合

今回はメール＋人の架電のハイブリッドで組んでいますが、電話そのものを自動化する余地は残しています。
架電リスト（P1）がそのまま発信対象リストになるので、IVRy / Twilio 等の音声AIに渡す形で拡張できます。
検討する場合の論点は `docs/runbook.md` の「AI音声架電への拡張」に整理しています。

---

# Google Meet 商談データの自動集約

第一営業部6名がGoogle Meetで商談した際、Gemini in Meetが自動生成する「メモ＋文字起こし」を、
**共有フォルダ「Google Meet」に毎日コピーで集約する**仕組み。

## 解こうとしている問題

Gemini in Meetのメモ／文字起こしは、デフォルトでは**商談した本人のDriveにしか残らない**。
他の営業マンや管理者が横断的に事例を探したり、ナレッジとして活用したりできない。

## 仕組み

```
毎日 7:30 (JST)
   │
   ├─ 集約フォルダ「Google Meet」の中身を確認（重複コピー防止のチェックリスト）
   │
   ├─ 第一営業部6名それぞれのDriveから、直近3日分の
   │     「Gemini によるメモ」ドキュメント（メモ・文字起こしタブを含む1ファイル）を検索
   │     社内MTG（config/team.json の exclude.titleContains に該当）は除外
   │
   └─ 未コピーのものだけ「[担当名] 元のタイトル」で集約フォルダにコピー
        元ファイルは各自のDriveにそのまま残る（削除・移動はしない）
```

## 構成

```
config/meet-collector.json                   設定。対象フォルダ・対象メンバー・検索条件
.claude/skills/meet-collector/SKILL.md        実行手順の本体
```

対象フォルダ: https://drive.google.com/drive/u/0/folders/1_Rr4LqPD6fHhcmXFX4LsEORuAJlZaPKG
（所有者: k_tsuchiya@writeup.co.jp、集約用アカウントに編集者権限で共有済み）

## 使い方

**手動実行**

```
Meetのデータ集めて
```

**自動実行**

毎日 7:30 (JST) の Routine から起動します。

## いまの制約

- Gemini in Meetは「メモ」タブと「文字起こし」タブを**同じドキュメント内**に生成する仕様のため、コピーもドキュメント単位（1商談＝1ファイル）になる。
- 集約は**コピー**であり、元ファイルの共有設定を変更するものではない。集約フォルダを見る人には、コピー時点の内容が渡る（その後の元ファイルの編集は反映されない）。
- 検索対象は `config/meet-collector.json` の `members[]` に列挙した6名のみ。増員した場合はここに追記する。
- **Routineは「新しいセッションを毎回立ち上げる」方式ではなく、Google Driveコネクタを持つ既存セッションに束縛する方式で動いている。**
  このワークスペースでは新規セッション生成のRoutineにGoogleコネクタを引き継げない制約があるため。
  そのセッションが何らかの理由でアーカイブ／終了された場合はRoutineが動かなくなるので、その際は同じ手順でRoutineを作り直す必要がある。
- **2026-08-31以降、この「1商談=1ファイルをコピー」方式の新規実行は停止した。** 過去に作られた個別コピー（集約フォルダ内の既存ファイル）はそのまま残す。代わりに下記「全商談を1ファイルに集約」方式に移行する。

## Google Meet 商談データの全期間集約（新方式・実証止まり）

上記の日次コピー方式とは別に、**過去〜現在の全商談の文字起こしを、少数のGoogleドキュメントに時系列で連結する**一括集約を2026-08-31に試みた。

### 分かったこと

`config/meet-collector.json` の6名全員について、`owner = <メール> and title contains 'Gemini によるメモ' and parentId != 集約フォルダID` で全期間検索したところ：

- 対象商談（社内MTG等を除外後）：**2,667件**
- 期間：2025-02-03 〜 2026-08-28（約18ヶ月）
- ソース合計サイズ：約73.6MB
- 担当別内訳：土屋721／村田660／菅野445／浦島324／吉川430／榎本87

この一覧・除外判定・日時ソート済みの結果は `config/meet-archive-manifest.json` の `fullScopeDiscovered` に保存済み。

### なぜ1回のセッションで完了できなかったか

`read_file_content` で内容を取得してそのまま `create_file` に書き戻す方式は、読み取りと同等量の出力トークンをもう一度消費するため、2,667件・73.6MB全件を1回のセッションで処理するのは非現実的だった。

**2026-08-31以降は `download_file_content`（`exportMimeType: text/plain`）を使う方式に変更した。** このツールは内容が一定サイズを超えるとローカルファイルへ自動保存され、会話コンテキストに内容を展開せずに済む。ローカルファイルの中身は `jq` と `base64 -d` で直接テキストファイルへ連結できるため、「読み取り」のコストがほぼゼロになる。コストが発生するのはチャンク（80万文字ごと）をGoogleドキュメントとしてアップロードする瞬間だけであり、これは避けられない下限コストとして許容している。

### 今の実装方針：日次Routineでバックフィルを継続

ユーザーの承認のもと、**過去分も含めて全2,667件を、複数回に分けてバックフィルする**方針にした。

- 上記「Google Meet 商談データの自動集約」の日次Routine（毎日7:30 JST）を、この全期間アーカイブの継続処理に転用した。
- 動作：`config/meet-archive-manifest.json` の `totalMeetingsIncluded` が `fullScopeDiscovered.includedCandidates`（対象総数）に届くまでは、1回の実行あたりトークン使用量300万を目安にバックフィルを進める。チャンクをアップロードするたびに manifest を更新し、git commit & push する（進捗を失わない）。
- 全件のバックフィルが完了したら、Routineは自動的に「新しく増えた商談だけを追記する」日次の定常運用モードに切り替わる（`includedSourceFileIds` に無いものだけを検索）。
- 進捗は `config/meet-archive-manifest.json` の `totalMeetingsIncluded` / `chunks` / `resumption` を見れば分かる。
