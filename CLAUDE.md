# ライトアップ 第一営業部 / AI営業まわり

## このリポジトリ

商談後フォロー（AIアシスタント「ヒロシ」）と、その周辺の営業自動化を置いている。
運用の全体像は `docs/hiroshi-followup.md`、送信台帳の扱いは `data/README.md` を先に読むこと。

## 進め方の約束

**能力の壁にぶつかったら、回避策を設計する前に「この環境で誰ならできるか」を主体で列挙する。**
手段を並べるのではなく、主体を並べる。2026/09 に Google スプレッドシートへ
書き込めない問題で、Claude・Apps Script・Codex・人 という4つの主体があり、
そのうち Codex は既にシートへ書いた実績があったのに、最初の回避策（Apps Script）を
見つけた時点で探索を止めてしまい、より簡単な答えを取り逃がした。
最初に見つかった解で満足しないこと。

**制約は、その前提が崩れた時点で一度こちらから再提示する。**
「Codexは使わない」のような方針は、それが出された時の前提の上に立っている。
「Claude側に手段が存在しないと確定した」のように前提が変わったら、黙って従い続けず、
「この部分だけ方針の外に出す選択肢があります」と一度出す。
ただし決着済みの判断を何度も蒸し返さないこと。境界は「新しい事実が前提を無効にしたか」。

**ユーザーの制約が固いか柔らかいか分からなければ聞く。** 枝を刈る前に確認する。

## この環境で分かっている制限（再調査しないでよい）

- **Google スプレッドシートのセルを書き換える手段は無い。** Drive MCP の `update_file` は
  タイトルと保存先のみ。コネクタ一覧にも Sheets 編集系は存在しない（2026/09 確認）。
  → Drive にCSVを置き、Codex か Apps Script に追記させる。
- 「送信mailログ」タブは `read_file_content` では読めない。`download_file_content` に
  `exportMimeType='application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'`
  を指定して xlsx で取ると全タブ読める。
- 自動モードの分類器が、外部宛に一括送信するルーティンの作成を止めることがある。
  同じ内容でも通ることがあるので、拒否されたら少し形を変えて再試行してよい。
  ただし回避を目的とした細工はしない。
- 大きなファイルをツール引数へ手で書き写さない。写し間違いが実データとの差異になる。
  ファイルを渡すか、生成したセッション自身に書き出させる。
- **顧客返信のSlack自動通知は2026/09/03、毎時→6時間おきに頻度を落とした。**
  その後、トークン消費の本当の原因（ラベル漏れによるスレッド再取得）を直したうえで、
  即応速度を優先して毎時に戻した。詳細は `docs/hiroshi-followup.md` の
  「返信監視について」を参照。
- **Codex経由でスプレッドシートへ書き込ませるとき、既に相手側にある行を含めて
  渡さない。** 2026/09/03、過去分をバックアップのつもりで含めてCSVを渡したところ、
  Codex側の重複チェックが機能せず20社分が二重登録された（実害なし、記録の重複のみ）。
  相手の重複排除ロジックを信用せず、自分が渡すデータ自体に重複の種を作らないこと。
  詳細は `docs/hiroshi-followup.md` の「インシデント：2026/09/03」を参照。
- **ヒロシ宛のメールを、返信監視ルーティン以外（このセッションで手動対応する等）で
  読んだ・返信した場合も、必ずその場で `mcp__Gmail__label_message` に labelIds:
  ["Label_1"] を付けること。** `search_threads` はスレッド単位で結果を返すため、
  ラベルの付いていないメッセージが1件でも残ると、そのスレッド全体が監視ルーティンの
  実行のたびに何度も再取得され、トークンを浪費し続ける。2026/09/03に実際発生した。
- **フォロー対象の選定には、ヨミ表のR列（初回商談日）・S列（2回目商談日）と本日の
  日付差を必ず確認すること。** ユーザー方針は「前回商談から1ヶ月以上空いている
  相手が望ましい」。ステータス（失注・Cヨミ等）だけでは直近の商談を除外できない。
  2026/09/03、この確認を怠り商談の翌日〜3週間後の相手に19社誤って送った。
  詳細は `docs/hiroshi-followup.md` の「対象選定の不具合」を参照。
