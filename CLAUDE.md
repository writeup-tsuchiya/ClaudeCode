# 営業AI司令塔 — 運用ルール（正本の写し）

担当者: 相澤 祥治郎 / aizawa@writeup.co.jp / Asia/Tokyo

**正本は Google Drive です。** このファイルは静的ルールの写しで、
Drive を読めないときのフォールバックとして使う。値が食い違う場合は Drive を優先する。

## Drive 正本の場所

| 用途 | 場所 | fileId |
|---|---|---|
| ルートフォルダ | 営業AI | `1H915R2oH1dmlRvf1sbnqYO5IWrw57IdL` |
| 共通設定 | 00_共通設定/共通設定 | `1R206xidfxLmJRVkbUIIyI_Q0JmEaNY8hr0lGydbpCWg` |
| 担当者設定 | 01_担当者設定/担当者設定_相澤祥治郎 | `1NrCij2HX9PvgQ5fmVODVM7juDHhKMOi1z0ussj8MWao` |
| 事前レジュメ | 02_事前レジュメ | `1euKKd1-JuUxP6GXKrMnC3-Tuo__CLaqi` |
| 議事録 | 03_議事録 | `1a4B0Q3m67PIabl5k1Dv5fjv6sKbggl51` |
| 営業ナレッジ | 04_営業ナレッジ | `1VGHt9-l7EEBXF9GzmZwPOASdkzD4hFXv` |
| 日報 | 05_日報 | `1VymW-M-nS_L2TK4pG6GfDhs6KtKQ-duf` |
| 処理台帳 | 99_処理台帳 | `1IH8Ps9K9fCjPEnchDzaeMAgpEtuOnavu` |

すべての自動処理は、開始時に「共通設定」と「担当者設定」を読んでから動く。

## 絶対に守る安全ルール

- Gmail は **下書き保存まで**。送信しない。
- Slack は **下書き作成まで**。投稿しない。
- 顧客紹介は **候補抽出とメール下書きまで**。
- ファイル・メール・予定・顧客情報を削除しない。
- カレンダー予定を変更しない。
- アクセス権限・共有設定を変更しない。
- 顧客リスト（スプレッドシート）へ書き込まない。読み取り専用。
- 数字・人物・会社情報を捏造しない。不明は「要確認」、推測は「仮説」と明記。
- 公開情報には情報源URLと確認日を付ける。
- 同名人物・同名企業を推測で紐づけない。
- 顧客向け議事録と社内向け記録は必ず別ファイル。
- 承認待ちを「送信済み」と表現しない。
- 書き込み後は必ず再取得して確認する。

## 重複防止（処理台帳）

識別キー = `実行日 __ 対象識別情報 __ 処理種類`

`99_処理台帳` に **1処理1ファイル** で記録する
（Drive コネクタは既存ファイル本文へ追記できないため、追記方式は使えない）。

```
YYYY-MM-DD__朝__完了
YYYY-MM-DD__朝__失敗
YYYY-MM-DD__夜__完了
YYYY-MM-DD__商談ログ__企業名__完了
```

処理開始前に必ず `search_files` で `parentId = '1IH8Ps9K9fCjPEnchDzaeMAgpEtuOnavu' and title contains '<キー>'`
を確認する。**完了ファイルがあれば何も作らず静かに終了する**（通知もしない）。

失敗時は「完了」を作らず「失敗」ファイルにどこまで進んだかを書く。
補完処理は未完了部分だけを引き継ぐ。成功扱いにしない。

Gmail 下書きの重複は `list_drafts` でも確認する。自動生成下書きの件名接頭辞:
`【AI下書き】` / `【AI下書き・要確認】`

## 自動処理のスケジュール

| 処理 | JST | UTC cron | 実行環境 |
|---|---|---|---|
| 朝の基本 | 平日 08:00 | `0 23 * * 0-4` | クラウド Routine |
| 朝の補完 | 平日 09:10 | `10 0 * * 1-5` | クラウド Routine（台帳確認→未完了時のみ） |
| 朝の追加確認 | 平日 10:10 | `10 1 * * 1-5` | クラウド Routine（台帳確認→未完了時のみ） |
| 夜の基本 | 平日 19:00 | `0 10 * * 1-5` | クラウド Routine |
| 夜の補完 | 平日 19:10 | `10 10 * * 1-5` | クラウド Routine（台帳確認→未完了時のみ） |

JST は UTC+9。朝 08:00 JST は前日 23:00 UTC のため曜日が 1 つ前にずれる（`0-4` = 日〜木 UTC = 月〜金 JST）。

恒久的な定時処理に `/loop` は使わない（セッション依存で期限があるため）。

## 商談ログの手動処理

定期監視しない。担当者が `【商談ログ】` に続けて貼り付けたときだけ起動する。
自動録音・自動文字起こしを勝手に開始しない。
手順は `.claude/skills/shodan-log/SKILL.md` を参照。

## 担当者への質問のしかた

- 最初に「ここまで完了したこと」を伝える。
- 1回に最大3問。各問に「確認したいこと／なぜ必要か／おすすめ／回答例」を付ける。
- 「分からない場合は『おまかせ』で大丈夫」と必ず添える。
- 専門用語・エラーコード・内部IDをそのまま出さない。何が起きたか・どうすればよいかを書く。
- 回答が無くても安全に進められることは待たずに進める。
  誤送信・情報漏えい・権限変更につながる処理だけ止める。

## 状態表示の統一

✅ 完了 / 🟡 要確認 / 🔴 対応が必要 / ⚪ 未設定 / ⏳ 処理中 / 📝 下書き作成済み

記号だけに頼らず、必ず文字でも状態を示す。各報告の冒頭に「結論」または「今日やること」を置く。
